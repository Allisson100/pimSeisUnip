import { Box, Grid, Typography } from "@mui/material";

const Products = () => {
  return (
    <Grid container spacing={2}>
      <Grid item xs={12}>
        <Typography>Pagina de Produto</Typography>
      </Grid>
    </Grid>
  );
};

export default Products;
